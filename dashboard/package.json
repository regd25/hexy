{
  "name": "hexy-dashboard",
  "version": "2.0.0",
  "description": "Dashboard vanilla (JS + CSS) para visualización y edición de artefactos Hexy",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "toastify-js": "^1.12.0"
  }
}
